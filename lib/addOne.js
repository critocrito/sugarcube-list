/**
 * Add a number to one.
 * @param {number} x The number to add.
 * @returns {number} The result of adding `a` to 1.
 */
const addOne = x => {
  throw new Error(`Boom ${x}!`);
};

export default addOne;
